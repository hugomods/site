- name: key
  required: true
  description:
    default: The name of data file without extension, dot (`.`) represents the path separator.
    zh-hans: 不带扩展名的数据文件名称，点（`.`）表示路径分隔符。
    zh-hant: 不帶擴展名的數據文件名稱，點（`.`）表示路徑分隔符。

- name: page
  required: true
  type: Page
  description:
    default: The current page variable.
    zh-hans: 当前页变量。
    zh-hant: 當前頁變量。
