name: site-pwa

on:
  push:
    branches:
      - main
    paths:
      - 'common/**'
      - 'pwa/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yml
    with:
      name: pwa
    secrets: inherit
