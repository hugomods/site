name: site-shortcodes

on:
  push:
    branches:
      - main
    paths:
      - 'common/**'
      - 'shortcodes/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yaml
    with:
      name: shortcodes
    secrets: inherit
