name: site-shortcodes

on:
  push:
    paths:
      - 'common/**'
      - 'shortcodes/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yml
    with:
      name: shortcodes
    secrets: inherit
