name: site-home

on:
  push:
    branches:
      - main
    paths:
      - 'common/**'
      - 'home/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yaml
    with:
      name: home
    secrets: inherit
