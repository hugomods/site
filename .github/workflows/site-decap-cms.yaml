name: site-decap-cms

on:
  push:
    paths:
      - 'common/**'
      - 'decap-cms/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yml
    with:
      name: decap-cms
    secrets: inherit
