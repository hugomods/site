name: site-images

on:
  push:
    branches:
      - main
    paths:
      - 'common/**'
      - 'images/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yaml
    with:
      name: images
    secrets: inherit
