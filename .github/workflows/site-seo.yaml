name: site-seo

on:
  push:
    branches:
      - main
    paths:
      - 'common/**'
      - 'seo/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yaml
    with:
      name: seo
    secrets: inherit
