name: site-blog

on:
  push:
    branches:
      - main
    paths:
      - 'common/**'
      - 'blog/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yaml
    with:
      name: blog
    secrets: inherit
