name: site-hugopress

on:
  push:
    paths:
      - 'common/**'
      - 'hugopress/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yml
    with:
      name: hugopress
    secrets: inherit
