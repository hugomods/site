name: site-hugopress

on:
  push:
    branches:
      - main
    paths:
      - 'common/**'
      - 'hugopress/**'

jobs:
  version-matrix:
    uses: ./.github/workflows/site.yaml
    with:
      name: hugopress
    secrets: inherit
